<div>
<h1> Make a new list </h1>

<%= simple_form_for @list do |f| %>
    <%= f.input :name %>
  <%= f.association :movies %>
  <%= f.association :bookmarks %>
  <%= f.button :submit %>
<% end %>
</div>
