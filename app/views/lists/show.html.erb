<h1> Lists details</h2>
<h2><%= @list.name %></h2>
<p class="btn btn-success btn-lg"> <%= link_to "<button>Back to Lists</button>".html_safe, lists_path %> </p>
<p><strong>movies</strong></p>
<ul>
  <% @list.movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>
<br>

<%# <p class="btn btn-info btn-lg"> <%= link_to "<button>Add a bookmark</button>".html_safe, new_list_bookmark_path %>
